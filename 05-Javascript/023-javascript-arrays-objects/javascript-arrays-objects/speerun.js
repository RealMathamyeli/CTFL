/*
- Programmieren mit JavaScript
*/

// Variable
// Datentypen (Code Organizieren)

// - Boolean - (true, false)
let isLoggedIn = true;

// - String - (mit "")
let userName = "Gloria";

// - Number
let score = 21;

// - Undefined
let temperature; // console.log(temperature) - output: undefined

// - Null
let initialWert = null; // console.log(initialWert) - output: null

// - Objekte (Arrays & Objekt) - Datenstrukturen (Data Structures)

// - Datentyp-methoden
// - String-Methoden (.trim(), .toUpperCase())
// - Number- Methoden (Number())

// Operatoren
// - Arithmetiche Operatoren = +,-,*,/,%
// - Vergleicheoperatoren = >, <, ===, !==
// - Logische Operatoren = && (AND - UND), || (OR - ODER), ! (NOT - NICHT)
// Conditionals (if-else)
// - if (if(bedienung){ // code})
// - if-else (if(bedienung){// code}else{ // code}  )
// - else-if (if(bedienung){// code}else if(bedienung){ // code}else{ // code})
// - Switch (switch(variable){ case: break; default;})
// Schleifen (loops)
// - do-while
// - while
// - for
// - for-each
// - map()
// Funktionen
// - Funktionsdeklaration (function nameDesFunktions(parameter){ Funktionskörper (function body)})
// - Pfeilefunktionen (const functionsName = (parameter)=> {Funktionskörper (function body)})
