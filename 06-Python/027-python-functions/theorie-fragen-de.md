# Fragen zu Python Funktionen und Modularisierung

1. **Was sind die fünf wesentlichen Komponenten einer Python-Funktionsdefinition?**
   - Erkläre, was jeder Teil (Schlüsselwort, Name, Klammern, Doppelpunkt und eingerückter Block) macht.

2. **Was ist der Unterschied zwischen einem Parameter und einem Argument?**
   - Gib ein Beispiel an, das beide Konzepte demonstriert.

3. **Was macht die `return`-Anweisung in einer Funktion?**
   - Beschreibe die zwei Dinge, die passieren, wenn eine `return`-Anweisung ausgeführt wird.

4. **Welchen Wert gibt eine Python-Funktion zurück, wenn sie keine explizite `return`-Anweisung hat?**

5. **Erkläre den Unterschied zwischen lokalen und globalen Variablen.**
   - Was passiert, wenn du versuchst, von außerhalb der Funktion auf eine lokale Variable zuzugreifen?

6. **Was ist der Zweck des `global`-Schlüsselworts?**
   - Warum solltest du vorsichtig sein, wenn du es verwendest?

7. **Was ist Modularisierung und warum ist sie in der Programmierung wichtig?**
   - Nenne mindestens drei Vorteile der Organisation von Code in Module.

8. **Wie importierst und verwendest du eine Funktion aus der Python-Standardbibliothek?**
   - Gib ein Beispiel mit dem `random`- oder `math`-Modul an.

9. **Was ist der Unterschied zwischen `import math` und `from math import sqrt`?**
   - Beschreibe, wie du die Quadratwurzel-Funktion mit jeder Import-Methode aufrufen würdest.

10. **Warum wird `from module import *` generell als schlechte Praxis angesehen?**
    - Welche Probleme kann diese Import-Methode in deinem Code verursachen?
